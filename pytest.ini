[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
; The suite is fully function-based; disabling class collection avoids a
; spurious warning when test files import model classes whose names
; happen to start with "Test" (e.g. the Testimonial model).
python_classes =
