services:
  web:
    build: .
    command: flask run --host=0.0.0.0 --port=8000 --debug
    environment:
      FLASK_ENV: development
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  db:
    ports:
      - "5432:5432"
